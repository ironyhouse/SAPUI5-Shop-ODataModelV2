<core:FragmentDefinition
	height="100%"
	xmlns:mvc="sap.ui.core.mvc"
	xmlns:core="sap.ui.core"
	xmlns="sap.m"
	xmlns:f="sap.f">
 	<List
		items="{oData>Products}"
		headerText="{i18n>suppliersTableTitle}">
		<ObjectListItem
			type="Active"
			title="{oData>Name}"
			number="{oData>Price}"
			numberUnit="{i18n>usd}"
			numberState="Success" >
		</ObjectListItem>
	</List>
</core:FragmentDefinition>